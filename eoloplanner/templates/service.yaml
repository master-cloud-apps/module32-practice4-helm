apiVersion: v1
kind: Service
metadata:
  name: {{ include "eoloplanner.fullname" . }}
  labels:
    {{- include "eoloplanner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eoloplanner.selectorLabels" . | nindent 4 }}
